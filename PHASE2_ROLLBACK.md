# SaaS Phase 2 回滚方案

## 1. 代码回滚

可回到 Phase 2 前 master 状态：

```bash
git checkout master
```

或回到 Phase 1 tag：

```bash
git checkout saas-phase1-register-ok
```

注意：

- 回滚代码不等于自动回滚云函数。
- 小程序体验版或云函数异常时，需要按部署平台重新上传/重新部署对应版本。

## 2. 云函数回滚

如果出现异常，按影响范围回滚。

### 2.1 `listPendingStageLogs` 异常

影响：

- 待审核日报列表加载异常。
- 项目名显示异常。

处理：

- 回滚待审核日报列表函数 `listPendingStageLogs`。
- 优先只回滚该函数，不扩大到其他云函数。

### 2.2 `createProject` 异常

影响：

- 新建项目异常。
- 晟景或测试租户创建项目被误拦截。

处理：

- 回滚项目创建函数 `createProject`。
- 同时检查晟景租户套餐额度是否足够。

### 2.3 `createStaffInviteCode` 异常

影响：

- 员工邀请码生成异常。
- 员工未满时也无法生成邀请码。

处理：

- 回滚员工邀请码函数 `createStaffInviteCode`。
- 不删除已有邀请码。

### 2.4 `bindStaffRole` 异常

影响：

- 员工激活异常。
- 旧邀请码激活异常。

处理：

- 回滚员工激活函数 `bindStaffRole`。
- 不删除已有员工账号。

## 3. 业务应急

如果晟景新增项目或员工被误拦截：

- 先检查晟景租户套餐额度。
- 不要删除历史项目。
- 不要删除历史员工。
- 不要批量改数据库。
- 优先人工调整晟景套餐额度。
- 必要时回滚相关云函数。

## 4. 数据处理原则

- 禁止批量删除数据。
- 禁止批量回填数据。
- 禁止重新运行 `initSaasDefaults`。
- 如确需修正套餐额度，只允许人工在控制台确认后手动修改目标租户记录。

## 5. 回滚后确认

```txt
回滚时间：
回滚人：
回滚云函数：
是否恢复晟景创建项目：
是否恢复员工邀请码：
是否恢复员工激活：
是否恢复待审核日报列表：
备注：
```
