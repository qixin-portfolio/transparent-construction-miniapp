# AGENTS.md

本文件定义 Codex 在本仓库内的执行规则。更高优先级的 system、developer、tool、安全和权限规则始终优先。

## 基本原则

1. 仓库是长期记忆，聊天只是临时界面。
2. 复杂任务必须先有 GitHub Issue，再有分支和 PR。
3. Codex 不得只在聊天中汇报结果，必须同步更新 PR 描述或 `AI_TASKS/handoff.md`。
4. 不允许自动合并 PR。
5. 不允许提交真实密钥、token、数据库连接串、账号密码。
6. 不允许为了完成任务绕过沙盒、权限、审批或安全要求。

## 默认工作流

1. 读取 Issue、PR 或 `AI_TASKS/current.md`，确认任务边界。
2. 从主分支创建任务分支，命名建议：`codex/<task-name>`。
3. 先审计，再设计，再小步修改。
4. 修改前先搜索现有实现，优先复用已有文件、函数、组件和约定。
5. 每次只做当前任务要求的范围，不夹带重构。
6. 完成后运行与改动匹配的检查；不能运行时说明原因。
7. 更新 `AI_TASKS/handoff.md` 和 PR 描述。

## 禁止默认执行的高风险动作

以下动作必须进入 Human Gate，等待用户明确确认：

- 部署、发布、上传体验版或正式版
- 数据库结构变更、数据迁移、批量写入、批量删除
- 支付、认证、权限、生产环境相关改动
- 删除数据、删除文件、重命名核心字段
- 引入真实 AI API、真实密钥、第三方生产服务
- 修改环境变量、部署配置、云函数生产配置

## 文档职责

- `AI_TASKS/current.md`：当前任务单。
- `AI_TASKS/handoff.md`：跨 ChatGPT、Codex、GitHub 的交接日志。
- `AI_TASKS/PROTOCOL.md`：三方协作协议。
- `AI_TASKS/LOOP_PROTOCOL.md`：循环工程执行协议。

## Codex 输出要求

最终回复应简洁说明：

- PR 链接或阻塞原因
- 修改文件
- 是否只改文档
- 工作区是否 clean
