# Provider Adapter

`mock` is the default and only configured provider. It validates and persists a real result file for the end-to-end test chain. A real provider remains gated by `STYLE_PREVIEW_REAL_AI_ENABLED=true`; no key or paid provider is selected in this branch.
