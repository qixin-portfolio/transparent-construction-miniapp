# 公开首页审核整改验证报告

验证日期：2026-07-28
实现基线：整改提交 `f2e2715`（起点 `821d96e`，审计提交 `dbe4a62`）

## 已完成的本地验证

| 检查 | 结果 |
| --- | --- |
| 公开入口路由与授权边界 | `node --test tests/public-home-entry.test.js`，`14/14` 通过 |
| 日报既有行为回归 | `node --test tests/stage-log-behavior/*.test.js`，`171/171` 通过 |
| 修改 JavaScript 语法 | `node --check` 通过 |
| 小程序 JSON | 全量解析通过 |
| 变更空白/冲突 | `git diff --check` 通过 |
| 示例工地隔离 | 静态扫描未发现 `wx.cloud`、云函数、手机号、头像或昵称授权调用 |
| 冻结入口 | `ENABLE_V2_DEAL_LOOP_ENTRY = false`；未发现新增量房风格预览入口 |

自动化覆盖的关键结论：未知用户进入公开首页而非老板注册；老板/员工保留工作台；已绑定业主按项目数量进入原详情或列表；未绑定页只重新检查权限；示例工地仅使用本地数据；头像昵称仍是可选资料编辑。

## 尚未完成的真实运行验证

微信开发者工具可打开本 worktree，但模拟器启动报错：

```text
41002 appid missing
```

`project.config.json` 内仍为 `wxbfe2172a118ae67f`，且未修改 `project.private.config.json`。该错误使以下证据尚未生成：

- 公开首页、未绑定说明页和示例工地的真实小程序截图；
- 已识别身份的真实启动分流截图；
- 审核前的完整模拟器运行验证；
- 体验版 `7.3.3` 上传结果。

不能用静态页面或桌面截图替代小程序运行截图，因此本轮未上传体验版、未 push、未创建 PR、未部署云函数，也未触碰生产数据。

## 恢复后的验证顺序

1. 在微信开发者工具恢复本 worktree 与 AppID 的关联并确认模拟器启动。
2. 截图公开首页与“我是工作人员”主动入口。
3. 截图未绑定项目说明页，并确认没有手机号、头像、昵称授权及前端绑定操作。
4. 截图示例首页、进度、日报、照片、设计确认、问题/验收页面。
5. 验证已绑定业主、员工、老板的实际启动分流。
6. 全部通过后，才上传体验版 `7.3.3` 并按实际结果更新本报告。
