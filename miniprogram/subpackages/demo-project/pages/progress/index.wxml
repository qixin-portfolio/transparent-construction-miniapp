<view class="demo-page"><view class="demo-note">示例数据，仅用于功能体验</view><view class="page-title">施工进度</view><view class="page-subtitle">整体完成 {{project.progress}}%，当前进行 {{project.stage}}</view><view class="section"><view wx:for="{{project.stages}}" wx:key="name" class="card"><view class="card-title">{{index + 1}}. {{item.name}}</view><view class="status {{item.active ? 'pending' : ''}}">{{item.status}}</view></view></view></view>
