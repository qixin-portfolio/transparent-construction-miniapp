# 晟景透明工地小程序 · UI 视觉升级总览

## 升级目标
将小程序从「简陋后台风格」升级为「现代、高级、可信赖、有品牌感的装修服务小程序」。

## 完成情况
全部 16 个页面 + 全局设计系统 + 自定义底部导航，均已升级完成。

---

## 一、全局设计系统（app.wxss）

### 色彩系统
| 用途 | 旧值 | 新值 |
|------|------|------|
| 主色 | `#1f6f54` | `#0F6A4A` |
| 辅助绿 | - | `#1F8B61` |
| 浅绿背景 | `#e4f1ea` | `#EAF7F1` |
| 页面背景 | `#f6f3ed`（米黄） | `#F7FAF8`（清白） |
| 卡片背景 | `#fffaf2`（米色） | `#FFFFFF`（纯白） |
| 主文字 | `#1f3f34` | `#111827` |
| 次文字 | `#718076` | `#6B7280` |
| 弱文字 | - | `#9CA3AF` |
| 边框 | `#e8deca`（米色） | `#E5E7EB`（冷灰） |
| 警示色 | `#b2432f` | `#D9534F` |
| 成功色 | - | `#16A34A` |
| 等待色 | - | `#F59E0B` |

### 圆角规范
| 元素 | 旧值 | 新值 |
|------|------|------|
| 大卡片 | 8rpx | 40rpx |
| 普通卡片 | 8rpx | 32rpx |
| 按钮 | 8rpx | 28rpx |
| 输入框 | 8rpx | 24rpx |
| 标签 | 8rpx | 999rpx（pill） |
| 图片 | 无 | 24rpx |

### 卡片样式
- 白底 + 柔和阴影 `0 4rpx 24rpx rgba(17,24,39,0.04)`
- 无粗边框，轻量呼吸感
- 渐变 Hero 区：`linear-gradient(135deg, #0F6A4A, #1F8B61)`

### 按钮规范
- 主按钮：`#0F6A4A` 白字，100rpx 高，带投影 `rgba(15,106,74,0.24)`
- 次按钮：`#EAF7F1` 底 `#0F6A4A` 字，88rpx 高
- 危险按钮：`#FEE2E2` 底 `#D9534F` 字

---

## 二、自定义底部导航（custom-tab-bar）

- 线性圆角风格图标（Feather 风格 SVG）
- 选中项图标 + 文字变绿 `#0F6A4A`
- 白色背景 + 顶部柔和阴影
- `pageLifetimes.show` 自动检测路由，**无需修改任何页面 JS**
- 高度 128rpx + 安全区适配

---

## 三、16 个页面升级详情

### 1. 工作台（员工端）
- 深绿渐变 Hero 欢迎区
- 2×2 数据概览卡（浮于 Hero 之上）
- 2×2 快捷操作网格（彩色图标背景）
- 员工激活弹窗现代化

### 2. 工作台（业主端）
- 渐变 Hero + 项目封面卡
- 大号进度数字 + 进度条
- 3 列数据概览
- 信任列表双卡
- 未绑定状态空状态设计

### 3. 工地/门店页（业主端）
- 品牌 Banner（晟景装饰 · 交城28年 · 整装定制设计）
- 渐变进度入口卡
- 2×2 品牌优势网格
- 抖音账号卡 / 案例卡 / 门店信息卡

### 4. 工地列表（员工端）
- 圆角搜索栏（客户端筛选，已接入 JS）
- 横向状态筛选标签
- 现代工地卡片 + 渐变进度条 + 状态标签
- 悬浮 FAB 新增按钮

### 5. 我的页
- 渐变用户信息大卡（头像 + 姓名 + 角色 + 状态）
- 分组菜单：工作管理 / 智能工具 / 我的服务
- 彩色图标背景的菜单项
- 角色权限控制显示

### 6. 业主进度页
- 绑定流程：渐变 Hero + 浮起绑定面板
- 进度展示：渐变项目封面卡 + 大号进度 + 3 列指标
- 订阅通知横幅
- 效果图确认卡 / 照片墙 / 时间线

### 7. 工地详情页（核心）
- 渐变管理端 Hero（进度 + 节点 + 指标）
- 设计图纸入口卡（带图标）
- 业主绑定区（大号绑定码 + 三按钮）
- 照片墙 2 列 + 阶段筛选
- 施工时间线（圆点 + 状态标签 + 内嵌照片）
- 展开/收起按钮

### 8. 上传日报
- 表单分组卡片化
- 每个字段独立卡片 + 标签提示
- 大号照片上传网格 + 删除按钮
- 照片添加占位卡（虚线边框）

### 9. 审核日报
- 待审核计数标签
- 审核卡：工地名 + 内容 + 风险提示 + 照片
- 底部双按钮（退回 / 通过，通过按钮更宽）

### 10. 客户库
- 圆角搜索栏（已接入 JS）
- CRM 客户卡片（姓名 + 阶段标签 + 电话/来源 + 成交状态）
- 成交状态颜色区分（绿/红/灰）
- 悬浮 FAB 新增按钮

### 11. 客户详情/新增
- 分组表单：客户信息 / 需求信息 / 跟进状态
- 每组独立卡片 + 分组标题
- 统一输入框样式

### 12. AI 助手
- 渐变 Hero（带 ✨ 图标）
- 横向模式切换标签（pill 风格）
- 工具表单卡 / 操作按钮组
- 生成结果卡 + 复制/保存
- 本机记录列表

### 13. 设计图纸
- 标题卡（带图标）
- 筛选区（类型 + 空间 pill 标签）
- 图纸大卡（400rpx 图片 + 类型徽章 + 确认徽章）
- 底部操作栏
- 上传弹窗（底部弹出 + 圆角）

### 14. 员工邀请码
- 角色选择 chip 网格
- 渐变邀请码大卡（88rpx 超大数字）
- 邀请码列表（状态颜色区分）
- 员工列表（渐变头像）
- 使用说明（暖色提示卡）

### 15. 新增工地
- 分组表单：项目信息 / 客户信息 / 人员配置
- 底部固定新建按钮（安全区适配）

### 16. 业主绑定
- 绑定流程：渐变 Hero + 浮起绑定面板
- 6 位绑定码大输入框
- 绑定结果展示

---

## 四、JS 修改清单（最小化）

仅修改 3 个 JS 文件，均为纯 UI 层增强，**未触碰任何接口/业务逻辑**：

| 文件 | 修改内容 |
|------|----------|
| `pages/projects/projects.js` | 添加客户端搜索筛选（`onSearch` / `onStatusFilter` / `applyFilter`） |
| `pages/customers/customers.js` | 添加客户端搜索（`onSearch` / `applySearch`） |
| `pages/profile/profile.js` | 添加分组导航方法（`goProjects` 等 `wx.navigateTo` 调用） |

**未修改的 JS 文件**：app.js、workbench.js、owner.js、project-detail.js、upload-log.js、review-log.js、customer-edit.js、ai-assistant.js、design-drawings.js、staff-manage.js、project-edit.js + 全部云函数

---

## 五、保证事项

1. ✅ 未改接口 — 云函数全部未动
2. ✅ 未改业务逻辑 — 仅 3 个 JS 文件加了 UI 层筛选/导航
3. ✅ 未改页面路由 — app.json pages/subpackages 结构不变
4. ✅ 未删字段 — 所有 {{}} 数据绑定完整保留
5. ✅ 未大改功能流程 — 所有 bindtap/bindinput/bindchange 事件完整保留
6. ✅ 旧色值 100% 清除 — 无 `#1f6f54` / `#fffaf2` / `#f6f3ed` / `#e8deca` 残留
