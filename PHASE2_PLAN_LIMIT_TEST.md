# SaaS Phase 2 套餐限制与租户隔离测试清单

## 1. 测试前提

- 当前分支：`saas-phase2-plan-limit`
- 本阶段只验证最小范围：
  - 免费版项目数限制
  - 免费版员工数限制
  - 待审核日报二次查项目的租户隔离
- 不执行数据库迁移脚本。
- 不执行 `initSaasDefaults`。
- 不批量回填 `tenantId`。
- 不批量删除测试数据。
- 不发布正式版。

## 2. 本地静态检查

需要执行：

```bash
node --check cloudfunctions/listPendingStageLogs/index.js
node --check cloudfunctions/createProject/index.js
node --check cloudfunctions/createStaffInviteCode/index.js
node --check cloudfunctions/bindStaffRole/index.js
```

预期：全部无输出且退出码为 0。

## 3. 验收场景

### 场景 1：免费版第 1-3 个项目可创建

步骤：

1. 使用免费版测试租户老板账号登录。
2. 连续创建 3 个项目。

预期：

- 3 个项目均创建成功。
- `projects` 中新增记录均带当前 `tenantId`。

### 场景 2：免费版第 4 个项目被拦截

步骤：

1. 在同一免费版测试租户下创建第 4 个项目。

预期：

- 创建失败。
- 云函数返回 `PLAN_PROJECT_LIMIT_REACHED`。
- 前端展示：`当前套餐最多可创建 3 个项目，请升级套餐后继续添加`。

### 场景 3：免费版第 1-3 个员工可添加

步骤：

1. 使用免费版测试租户老板账号生成员工邀请码。
2. 使用 3 个不同微信账号完成员工激活。

预期：

- 3 个员工均可激活成功。
- 员工角色为 `worker` / `project_manager` / `designer` / `sales` 等内部员工角色。
- 老板本人不计入 3 个员工名额。

### 场景 4：免费版第 4 个员工被拦截

步骤：

1. 在同一免费版测试租户已有 3 名内部员工后，再生成员工邀请码或用旧邀请码激活第 4 名员工。

预期：

- 生成邀请码或激活员工被拦截。
- 云函数返回 `PLAN_STAFF_LIMIT_REACHED`。
- 前端展示：`当前套餐最多可添加 3 名员工，请升级套餐后继续添加`。

### 场景 5：晟景老账号创建项目不报错

步骤：

1. 使用晟景老管理员账号登录。
2. 尝试新建项目。

预期：

- 如果默认租户 `subscriptions` 已设置较高 `maxProjects`，创建不被免费版限制。
- 如果被免费版限制，说明默认租户缺少有效套餐记录或字段不完整，需要人工设置更高套餐。
- 不通过代码自动创建或回填套餐记录。

### 场景 6：`listPendingStageLogs` 仍能显示当前租户待审核日报

步骤：

1. 当前租户员工提交日报。
2. 管理员进入审核列表。

预期：

- 当前租户待审核日报正常显示。
- 项目名仍按原接口结构返回。

### 场景 7：A 租户不会通过待审核列表查到 B 租户项目名

步骤：

1. 准备 A 租户和 B 租户项目及待审核日报。
2. A 租户管理员进入待审核日报列表。

预期：

- A 租户只能看到 A 租户待审核日报。
- 二次查询项目时带 A 租户 `tenantId`。
- 不会把 B 租户项目名补到 A 租户日报记录上。

## 4. 风险记录

- 当前员工管理主流程从 `users` 读取员工列表，`bindStaffRole` 也只更新 `users`，因此本阶段员工数量统计按 `users` 集合内部角色计算。
- `tenant_users` 后续需要在更完整的租户成员模型中统一，但本阶段不做标准化迁移。
- 默认租户历史数据如果套餐字段缺失，可能按免费版限制处理，需要人工补套餐字段，不自动回填。
