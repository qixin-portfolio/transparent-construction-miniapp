# V4 正式版发布前人工验收结果

## 1. 当前基线

- 当前分支：`master`
- 当前 commit：`bbbdd95 merge: add V4 release preflight checklist`
- 体验版版本：`4.0.0`
- 正式版发布：否
- 数据库脚本：未执行
- `initSaasDefaults`：未执行

## 2. SaaS 配置确认结果

| 配置项 | 确认结果 |
|--------|---------|
| 晟景 tenantId | 已存在（具体 ID 见数据库） |
| plan | `enterprise` |
| status | `active` |
| maxProjects | `9999` |
| maxStaff | `9999` |

结论：晟景老租户套餐额度符合预期，已人工确认。

## 3. AI 配置确认结果

| 配置项 | 确认结果 |
|--------|---------|
| AI_API_KEY | 已配置（云函数环境变量，未写入代码） |
| AI_BASE_URL | 已配置（DeepSeek base url） |
| AI_MODEL | 已配置（deepseek-chat） |
| AI 调用是否成功 | 已验证（管理员调用成功） |
| AI 失败时不影响审核 | 已验证（try/catch 降级） |

结论：AI 环境变量已完整配置，管理员调用已验证通过。

## 4. 权限确认结果

| 权限项 | 确认结果 |
|--------|---------|
| manager 不能改套餐 | 代码侧已验证（白名单不含 manager） |
| worker 不能调用 AI 摘要 | 真机已验证（前端隐藏 + 后端白名单） |
| owner 不能调用 AI 摘要 | 真机已验证（前端隐藏 + 后端白名单） |
| adminUpdateTenantPlan 白名单正确 | `['admin', 'boss_qi', 'boss_hu', 'platform_admin', 'super_admin']` |
| aiGenerateOwnerSummary 白名单正确 | `['admin', 'boss_qi', 'boss_hu']` |

结论：权限配置正确，前后端双层安全控制。

## 5. 真机流程验收结果

| 验收场景 | 结果 | 备注 |
|---------|------|------|
| 新老板注册公司 | 通过 | Phase 1 已验证 |
| 新业主扫码绑定工地 | 通过 | Phase 1 已验证 |
| 员工/工长扫码加入 | 通过 | Phase 1/2 已验证 |
| 老板创建项目 | 通过 | Phase 2 已验证 |
| 免费版项目数限制（3个） | 通过 | Phase 2 已验证 |
| 免费版员工数限制（3个） | 通过 | Phase 2 已验证 |
| 老板查看套餐卡片 | 通过 | Phase 3 已验证 |
| 管理员手动升级套餐 | 通过 | Phase 3 已验证 |
| 工长上传日报和照片 | 通过 | 照片修复已验证 |
| 老板审核日报，照片正常显示 | 通过 | 照片修复已验证 |
| 老板 AI 生成业主摘要 | 通过 | Phase 4C 已验证 |
| 老板编辑摘要并审核通过 | 通过 | Phase 4C 已验证 |
| 业主端查看审核后的日报摘要和照片 | 通过 | Phase 1-4 综合验证 |
| worker / owner 无法调用 AI 摘要 | 通过 | 真机补测通过 |
| 公开案例/完工分享/门店页不误跳公司注册 | 通过 | Phase 1 已验证 |

## 6. 当前遗留问题

| 问题 | 状态 | 是否阻塞发布 |
|------|------|------------|
| stash@{0} 遗留文件 | 保留待处理 | 不阻塞 |
| geo-content-center/ 已在 master | 已进入 Phase 3 分支历史，不影响小程序构建 | 不阻塞 |
| 照片显示问题 | 已修复（photo-display-fix-ok） | 不阻塞 |
| 平台后台管理页面 | 未做 | 不阻塞（可通过云函数手动管理） |
| 支付接入 | 未接入 | 不阻塞（当前为手动管理套餐） |
| 文件路径按 tenantId 隔离 | 未做 | Phase 3 已识别风险，不阻塞 |
| owner_bindings 标准化 | 未做 | 不阻塞 |

## 7. 发布建议

当前版本具备提交正式版/开放第一批装修公司试用条件。

建议先进行小范围正式发布或灰度试用，不建议直接大规模铺开。

发布前建议先人工确认第 5 节中标记为"通过"的验收项，在发布前再走一遍核心流程，确保最后一次部署无回退。
