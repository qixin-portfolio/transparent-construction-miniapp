# SaaS Phase 3 验收文档 — 手动套餐管理

## 场景 1：老板查看套餐卡片

**前置**：老板账号登录（admin / boss_qi / boss_hu）

**操作**：进入工作台

**预期**：
- 工作台正常打开
- 老板驾驶舱尾部显示"当前套餐"卡片
- 卡片展示套餐名称、套餐状态、项目额度、员工额度
- 不影响原有工作台概览数据、异常提醒、员工排行、最新动态

## 场景 2：免费版额度展示

**前置**：免费租户老板登录

**预期**：
- planName 显示"免费试用版"
- statusText 显示"试用中"
- maxProjects 显示 3
- maxStaff 显示 3
- 进度条百分比根据实际使用量计算

## 场景 3：接近上限提示

**前置**：免费租户已有 2 个项目或 2 名员工

**预期**：
- 进度条显示黄色（warn 类）
- 卡片底部显示"额度快用完了，如需增加项目或员工，请联系服务顾问升级套餐。"

## 场景 4：已达上限提示

**前置**：免费租户已有 3 个项目或 3 名员工

**预期**：
- 进度条显示红色（danger 类）
- 卡片底部显示"当前套餐额度已用完，请联系服务顾问升级套餐。"

## 场景 5：平台管理员手动升级套餐

**前置**：admin / boss_qi / boss_hu 账号登录

**操作**：调用 `adminUpdateTenantPlan`，将测试租户从 free 改为 pro

**预期**：
- 返回 `success: true`
- `subscriptions` 中该租户 plan 更新为 pro
- `tenants` 中该租户 plan 更新为 pro
- 老板端刷新后套餐卡片显示"专业版"

## 场景 6：普通员工无权修改套餐

**前置**：worker / designer / sales / project_manager 账号登录

**操作**：直接调用 `adminUpdateTenantPlan`

**预期**：
- 返回 `success: false`
- message 为"无权限执行此操作"
- subscriptions 和 tenants 中套餐数据不变

## 场景 7：业主无权修改套餐

**前置**：owner 账号登录

**操作**：直接调用 `adminUpdateTenantPlan`

**预期**：
- 返回 `success: false`
- 不修改任何套餐数据

## 场景 8：套餐升级后 Phase 2 限制联动

**前置**：免费租户已有 3 个项目

**操作**：
1. 尝试创建第 4 个项目
2. 管理员升级套餐（pro，maxProjects: 100）
3. 再次尝试创建第 4 个项目

**预期**：
- 升级前：`PLAN_PROJECT_LIMIT_REACHED`
- 升级后：第 4 个项目可以创建

## 场景 9：异常回退检查

**前置**：老板账号登录

**操作**：模拟 getCurrentTenantPlan 调用失败（如断网）

**预期**：
- 工作台正常打开
- 不显示套餐卡片
- 其他模块（概览、驾驶舱、快捷操作）不受影响
- 控制台记录错误但不影响用户体验
