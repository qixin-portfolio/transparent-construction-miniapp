# SaaS Phase 2 体验版验收文档

## 1. 验收范围

本次只验收 Phase 2 最小范围：

- 免费版最多 3 个项目
- 免费版最多 3 个员工
- `listPendingStageLogs` 二次查询 `projects` 带 `tenantId`

不验收套餐购买页、支付、超级后台、文件路径改造、`owner_bindings` 标准化。

## 2. 验收前提

- 已人工确认晟景老租户套餐额度足够高。
- 只部署以下云函数：
  - `listPendingStageLogs`
  - `createProject`
  - `createStaffInviteCode`
  - `bindStaffRole`
- 未执行数据库迁移脚本。
- 未执行 `initSaasDefaults`。
- 未发布正式版。

## 3. 验收场景

### 场景 1：晟景老账号创建项目

预期：

- 如果晟景套餐额度已设置足够高，可以正常创建项目。
- 不出现 `PLAN_PROJECT_LIMIT_REACHED`。
- 原有创建项目流程不受影响。

记录：

```txt
测试账号：
测试时间：
测试结果：
备注：
```

### 场景 2：测试免费租户创建第 1-3 个项目

预期：

- 第 1 个项目可创建。
- 第 2 个项目可创建。
- 第 3 个项目可创建。

记录：

```txt
测试 tenantId：
第 1 个项目结果：
第 2 个项目结果：
第 3 个项目结果：
备注：
```

### 场景 3：测试免费租户创建第 4 个项目

预期：

- 被拦截。
- 返回错误码：`PLAN_PROJECT_LIMIT_REACHED`。
- 提示文案：`当前套餐最多可创建 3 个项目，请升级套餐后继续添加`。

记录：

```txt
测试 tenantId：
错误码：
提示文案：
测试结果：
备注：
```

### 场景 4：测试免费租户添加第 1-3 个员工

预期：

- 第 1 个员工可添加。
- 第 2 个员工可添加。
- 第 3 个员工可添加。

记录：

```txt
测试 tenantId：
第 1 个员工结果：
第 2 个员工结果：
第 3 个员工结果：
备注：
```

### 场景 5：测试免费租户添加第 4 个员工

预期：

- 创建邀请码或激活员工时被拦截。
- 返回错误码：`PLAN_STAFF_LIMIT_REACHED`。
- 提示文案：`当前套餐最多可添加 3 名员工，请升级套餐后继续添加`。

记录：

```txt
测试 tenantId：
触发入口：创建邀请码 / 激活员工
错误码：
提示文案：
测试结果：
备注：
```

### 场景 6：旧员工邀请码绕过测试

预期：

- 即使之前已经生成过邀请码。
- 如果当前员工数已达到上限。
- 员工扫码激活时仍然被 `bindStaffRole` 拦截。

记录：

```txt
测试 tenantId：
旧邀请码：
当前员工数：
错误码：
测试结果：
备注：
```

### 场景 7：待审核日报列表

预期：

- 当前租户仍能正常看到自己的待审核日报。
- 项目名称正常显示。

记录：

```txt
测试 tenantId：
待审核日报数量：
项目名称是否正常：
测试结果：
备注：
```

### 场景 8：跨租户隔离测试

预期：

- A 租户不会通过待审核日报列表查到 B 租户项目名。
- `projects` 二次查询必须带 `tenantId`。

记录：

```txt
A 租户 tenantId：
B 租户 tenantId：
A 租户审核列表结果：
是否出现 B 租户项目名：
测试结果：
备注：
```

## 4. 验收结论

```txt
验收时间：
验收人：
是否通过：
阻塞问题：
后续处理：
```
