# 透明工地 V2：成交闭环版 - Phase 6-L 销售动作卡体验版决策

## 1. 当前阶段

Phase 6-L：销售下一步动作卡体验版再次验证决策

## 2. 当前 HEAD / tag

- 当前 HEAD：383bf4065b22c6399cd04ca340508d830f8abbc6
- 当前 tag：v2-deal-loop-phase6k-sales-next-action-acceptance
- 当前安全状态：入口关闭，未上传体验版，未发布正式版

## 3. 本阶段目标

基于 Phase 6-J 销售下一步动作卡实现与 Phase 6-K 本地验收结果，判断是否值得再次给 admin + boss_qi + boss_hu 上传受控体验版。

本阶段只做决策文档，不修改代码，不打开入口，不上传体验版，不部署云函数，不发布正式版。

## 4. 当前安全状态

当前安全状态确认：

- `ENABLE_V2_DEAL_LOOP_ENTRY = false`
- 工作台入口默认隐藏
- 未上传体验版
- 未部署云函数
- 未发布正式版
- 未新增写库
- 未新增真实 AI
- 未调用 `createProject`
- 未新增 `wx.request`
- 未自动发送消息
- 未自动发布内容

## 5. 本轮新增价值

Phase 6-J 已新增销售下一步动作卡。

`pipeline` 客户卡片新增：

- 下一步动作
- 跟进重点
- 提示

`customer-detail` 新增：

- 当前判断
- 建议动作
- 可复制跟进话术

复制能力：

- 仅复制到剪贴板
- 不发送消息
- 不写库
- 不请求接口
- 不上传文件

本轮能力比单纯反馈模块更接近真实成交跟进场景。

## 6. 是否值得再次体验

建议进行一次 admin + boss_qi + boss_hu 三账号受控体验版验证。

理由：

- 这次新增的是老板/销售更容易感知价值的功能
- 比单纯反馈模块更接近真实成交场景
- 风险较低，只读展示 + 剪贴板复制
- 可以验证老板是否觉得“下一步动作卡”真的有用
- 仍然不涉及真实 AI、写库、创建工地、自动发布

本阶段不执行上传，只记录决策。

## 7. 体验目标

本轮体验重点验证：

1. 客户列表里的“下一步动作”是否一眼能看懂
2. 老板是否觉得这个提示对跟进客户有帮助
3. 客户详情里的“下一步跟进动作”是否实用
4. 复制出来的话术是否能直接发给客户
5. 是否还会误解为真实 AI
6. 是否还会误解为自动跟进
7. 是否还会误解为自动创建工地
8. 下一步是否应该继续增强销售跟进

## 8. 体验对象

本轮仍限定：

- admin
- boss_qi
- boss_hu

明确不开放：

- 业主端
- 工长端
- 普通销售
- 外部装修公司客户
- 真实业主
- 非测试人员

## 9. 体验版备注文案

可直接复制到微信开发者工具体验版备注：

透明工地 V2 成交跟进试验功能。本次仅限 admin、boss_qi、boss_hu 内部体验，重点验证新增的“销售下一步动作卡”和“复制跟进话术”是否有实际帮助。本版本只读客户资料，不创建真实工地，不调用真实 AI，不自动发送消息，不自动发布内容，不影响现有客户、工地和日报数据。请勿转发给非测试人员。体验结束后关闭入口。

## 10. 反馈收集问题

本轮专用反馈问题：

1. 客户列表里的“下一步动作”你能看懂吗？
2. 这个“下一步动作”对你判断客户跟进有没有帮助？
3. “跟进重点 / 提示”有没有多余或看不懂的地方？
4. 客户详情里的“下一步跟进动作”有没有用？
5. 复制出来的跟进话术是否像真人说话？
6. 复制话术你愿不愿意真的发给客户？
7. 有没有误以为它是真实 AI 自动生成？
8. 有没有误以为它会自动发送消息？
9. 有没有误以为它会创建真实工地？
10. 如果继续做，你希望它下一步增强什么？

## 11. 下一阶段建议

如果进入下一阶段，建议：

Phase 6-M：销售动作卡三账号体验版短期打开

执行顺序：

1. 将 `ENABLE_V2_DEAL_LOOP_ENTRY` 短期改为 `true`
2. 提交入口打开 commit/tag
3. 人工上传体验版
4. 只通知 admin / boss_qi / boss_hu
5. 上传后立即进入关闭入口阶段
6. 恢复 `ENABLE_V2_DEAL_LOOP_ENTRY = false`
7. 基于关闭入口版本重新上传体验版覆盖
8. 收集三账号反馈
9. 再决定是否继续增强销售跟进

## 12. 风险边界

风险边界：

- 不发布正式版
- 不部署云函数
- 不开放给其他人员
- 不新增真实 AI
- 不新增写库能力
- 不调用 `createProject`
- 不自动发送消息
- 不自动创建工地
- 不自动发布案例
- 体验窗口建议 1 天以内
- 体验结束必须关闭入口并覆盖上传关闭版

## 13. 发布判断

当前发布判断：

- 当前不打开入口
- 当前不上传体验版
- 当前不部署云函数
- 当前不发布正式版
- 当前不扩大体验范围

只有进入 Phase 6-M 并完成短期打开入口 commit/tag 后，才可由人工上传体验版。

## 14. 是否建议进入 Phase 6-M

建议进入 Phase 6-M：销售动作卡三账号体验版短期打开。

Phase 6-M 必须保持：

- 短期打开入口
- 入口仍由 `ENABLE_V2_DEAL_LOOP_ENTRY && isBoss` 控制
- 只通知 admin / boss_qi / boss_hu
- 体验窗口 1 天以内
- 不部署云函数
- 不发布正式版
- 上传后立即进入关闭入口阶段
- 关闭入口后覆盖上传关闭版
