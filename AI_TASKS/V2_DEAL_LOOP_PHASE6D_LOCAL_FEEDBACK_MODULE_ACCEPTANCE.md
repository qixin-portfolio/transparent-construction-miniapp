# 透明工地 V2：成交闭环版 - Phase 6-D 本地临时入口验收

## 1. 当前阶段

Phase 6-D：反馈模块本地临时入口验收

## 2. 当前 HEAD / tag

- 阶段执行前 HEAD：d8b788a78989fba64783c6ce2ecf720b626bcfde
- 前置 tag：v2-deal-loop-phase6c-feedback-followup-acceptance
- 当前安全基线：Phase 6-C 已完成并锁点

## 3. 本阶段目标

本阶段目标是在本地开发环境临时打开 V2 成交跟进入口，验收 Phase 6-B 新增的反馈采集模块和销售跟进提示模块是否能正常进入、正常显示、文案安全、复制按钮可用。

本阶段不是上传体验版，不是部署云函数，不是发布正式版。

## 4. 本地临时开关操作

本阶段按验收流程曾在本地临时执行：

- 将 `miniprogram/pages/workbench/workbench.js` 中的 `ENABLE_V2_DEAL_LOOP_ENTRY = false` 临时改为 `true`
- 用于本地查看工作台 V2 成交跟进入口
- 验收结束后已立即恢复为 `ENABLE_V2_DEAL_LOOP_ENTRY = false`

最终提交状态确认：

- `ENABLE_V2_DEAL_LOOP_ENTRY = false`
- `git diff` 中未保留入口打开状态
- 工作台入口默认仍隐藏
- 本阶段不提交 `workbench.js`

## 5. 工作台入口验收结果

本地临时打开入口后，工作台 V2 成交跟进入口卡片验收通过。

入口卡片显示内容符合 Phase 5E-C 的安全文案要求：

- 标题：成交跟进
- 标签：V2 试验功能
- 标签：仅老板内部体验
- 标签：只读客户资料
- 说明：用于内部查看客户成交跟进建议，不影响现有客户、工地和日报数据。
- 风险提示：当前为试验功能，不创建真实工地，不调用真实 AI，不自动发布内容。

入口仍通过 `canViewDealLoopV2` 控制，显示条件保持：

```js
ENABLE_V2_DEAL_LOOP_ENTRY && isBoss
```

角色范围未扩大，仍为：

- admin
- boss_qi
- boss_hu

## 6. pipeline 反馈模块验收结果

`pipeline` 页面新增模块验收通过。

“本轮体验反馈”模块正常显示，内容清楚直接：

1. 你能一眼看懂今天应该重点跟进谁吗？
2. 当前客户阶段和跟进建议是否清楚？
3. 哪个信息最能帮助你判断成交机会？

“复制反馈问题”按钮可用，用于复制本轮体验反馈问题文本。

“销售跟进提示”模块正常显示，内容为：

- 优先看高意向客户
- 先确认客户顾虑
- 再准备信任证据
- 最后推进下一步动作

页面仍保持内部参考语气，未发现“正式功能”“自动成交”“真实 AI 结论”等误导表达。

## 7. customer-detail 复盘模块验收结果

`customer-detail` 页面新增模块验收通过。

“成交跟进复盘”模块正常显示，4 个复盘问题清楚、实用：

1. 这个客户当前最大的顾虑是什么？
2. 下一步应该约沟通、补资料，还是推进签约？
3. 需要给客户看哪些工地证据或案例？
4. 当前建议有没有误导或看不懂的地方？

“复制客户反馈问题”按钮可用，用于复制当前客户的复盘问题文本。

销售助手建议仍明确为：

- 当前为示例建议，不调用真实 AI。
- 下一步跟进建议仅供内部参考，请人工判断后使用。

未发现写入跟进记录、自动发送、自动创建工地、自动发布内容的暗示。

## 8. 复制能力验收

两个复制按钮均只使用本地剪贴板能力：

- `pipeline.copyFeedbackQuestions`
- `customer-detail.copyCustomerFeedbackQuestions`

复制能力边界确认：

- 仅调用 `wx.setClipboardData`
- 不写数据库
- 不请求接口
- 不上传文件
- 不产生客户数据变更
- 不新增云函数调用

## 9. 入口关闭恢复确认

验收结束后已恢复：

```js
const ENABLE_V2_DEAL_LOOP_ENTRY = false
```

最终状态确认：

- `ENABLE_V2_DEAL_LOOP_ENTRY = false`
- 工作台入口默认隐藏
- `git diff` 中未保留 true 状态
- 本阶段只提交验收文档

## 10. 未改变的能力边界

本阶段未改变以下能力边界：

- 未修改 `cloudfunctions/`
- 未修改 `miniprogram/app.json`
- 未修改 tabBar
- 未修改 V2 其它页面
- 未新增数据库写入
- 未新增真实 AI API
- 未调用 `createProject`
- 未调用 `submitStageLog`
- 未调用 `reviewStageLog`
- 未调用 `getTempFileURL`
- 未新增 `wx.request`
- 未部署云函数
- 未上传体验版
- 未发布正式版

V2 仍保持：

- V2 试验功能
- 只读客户资料
- 内部成交跟进
- 不调用真实 AI
- 不创建真实工地
- 不自动发布内容
- 不影响现有客户、工地和日报数据

## 11. 发布判断

当前不建议发布正式版。

原因：

- 本阶段仅完成本地临时入口验收
- 入口已恢复关闭
- 反馈模块尚未经过新一轮受控体验版真实反馈
- 仍不应扩大到真实业务人员或外部客户

当前判断：

- 不打开入口
- 不上传体验版
- 不部署云函数
- 不发布正式版
- 不给真实业务人员大范围使用

## 12. 是否建议进入 Phase 6-E

可以进入 Phase 6-E，但建议 Phase 6-E 仍保持保守边界。

建议 Phase 6-E 目标：

- 只做反馈模块体验版再次验证准备或决策评审
- 明确是否需要再次短期打开入口
- 明确测试账号和体验窗口
- 明确体验后必须恢复入口关闭
- 不直接发布正式版

Phase 6-E 不应直接扩大入口范围，不应接入真实 AI，不应新增写库能力，不应进入真实 `createProject`。
