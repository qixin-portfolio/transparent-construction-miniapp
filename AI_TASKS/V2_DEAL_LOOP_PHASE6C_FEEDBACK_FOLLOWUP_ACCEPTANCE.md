# V2 Deal Loop Phase 6-C：反馈提示人工验收与优先级复核

## 当前阶段

Phase 6-C：反馈提示人工验收与优先级复核。

本阶段只做 Phase 6-B 新增反馈采集与销售跟进提示的验收与优先级复核，不新增功能，不打开入口，不上传体验版，不部署云函数，不发布正式版。

## 当前 HEAD / tag

- 当前 HEAD：`e4d67a5ec59bc950cbfa031963616149744e1e3f`
- 当前 tag：`v2-deal-loop-phase6b-feedback-followup-mini-implementation`
- 当前入口状态：`ENABLE_V2_DEAL_LOOP_ENTRY = false`

## 验收目标

本阶段验收目标：

1. 检查 `pipeline` 新增反馈采集模块是否清楚。
2. 检查 `customer-detail` 新增成交跟进复盘模块是否清楚。
3. 检查复制按钮是否只使用剪贴板能力。
4. 检查文案是否有误导真实 AI、自动成交、创建工地、自动发布或正式功能风险。
5. 复核 Phase 6-A 是否已有独立文档和 tag。
6. 给出下一阶段建议。

## pipeline 验收结果

验收结论：通过。

已确认新增模块：

1. `本轮体验反馈`
2. `复制反馈问题`
3. `销售跟进提示`

反馈说明文案：

`看完成交跟进后，请老板重点反馈：是否看得懂、哪个客户最值得跟、下一步动作是否清楚。`

3 个反馈问题：

1. `你能一眼看懂今天应该重点跟进谁吗？`
2. `当前客户阶段和跟进建议是否清楚？`
3. `哪个信息最能帮助你判断成交机会？`

验收判断：

1. 问题简单直接，能帮助老板明确反馈方向。
2. “哪个客户最值得跟”“下一步动作是否清楚”能对应销售跟进优先级。
3. “销售跟进提示”使用老板/销售能理解的表达。
4. 文案保持内部参考语气。
5. 未发现自动成交、正式功能、创建工地或自动发布暗示。

销售跟进提示内容：

1. `优先看高意向客户`
2. `先确认客户顾虑`
3. `再准备信任证据`
4. `最后推进下一步动作`

## customer-detail 验收结果

验收结论：通过。

已确认新增模块：

1. `成交跟进复盘`
2. `复制客户反馈问题`

复盘说明文案：

`进入客户详情后，先确认顾虑、下一步动作和需要补充的信任材料。`

4 个复盘问题：

1. `这个客户当前最大的顾虑是什么？`
2. `下一步应该约沟通、补资料，还是推进签约？`
3. `需要给客户看哪些工地证据或案例？`
4. `当前建议有没有误导或看不懂的地方？`

验收判断：

1. 问题能帮助销售围绕顾虑、动作、信任材料做复盘。
2. “当前建议有没有误导或看不懂的地方”能收集文案风险反馈。
3. 销售助手建议已明确为示例建议。
4. 页面提示“下一步跟进建议仅供内部参考，请人工判断后使用”。
5. 未发现写入跟进记录的暗示。
6. 未发现自动决策、自动发送、自动创建或自动发布暗示。

## 复制能力验收

复制能力验收结论：通过代码验收。

已确认：

1. `pipeline` 的 `复制反馈问题` 绑定 `copyFeedbackQuestions`。
2. `customer-detail` 的 `复制客户反馈问题` 绑定 `copyCustomerFeedbackQuestions`。
3. 两个方法均仅使用 `wx.setClipboardData`。
4. 两个方法成功后仅使用 `wx.showToast` 做本地提示。
5. 未写数据库。
6. 未请求接口。
7. 未上传文件。
8. 未产生客户数据变更。

备注：

本阶段未上传体验版，未打开入口；复制按钮以代码级验收为准。后续如再次进入开发者工具人工验收，可补充实际点击截图记录。

## 文案风险检查

文案风险检查结论：通过，保留一项限制说明。

未发现以下用户可见能力宣传文案：

```text
自动成交
AI 自动判断
一键创建工地
自动创建工地
自动发布
可直接发布
正式功能
正式上线
createProject
db.collection
wx.request
Phase 3B
Readonly
```

说明：

1. 页面出现 `不调用真实 AI`，这是限制说明，不是宣传真实 AI 能力。
2. 页面存在 `mock-btn / mock-note` 等 class 名和内部路径里的 `mock`，不属于用户可见文案。
3. 页面继续使用 `内部参考 / 示例建议 / 人工判断 / 只读客户资料 / 不影响现有数据 / 复制反馈问题` 等安全表达。

## Phase 6-A 状态说明

已确认 Phase 6-A 独立锁点存在。

文档：

`AI_TASKS/V2_DEAL_LOOP_PHASE6A_FEEDBACK_PRIORITY_DESIGN.md`

tag：

`v2-deal-loop-phase6a-feedback-priority-design`

说明：

1. Phase 6-A 已完成真实反馈驱动的功能优先级设计。
2. Phase 6-B 按 Phase 6-A 的低风险方向先行实现反馈采集与销售跟进增强。
3. 本阶段未发现高风险越界。
4. 后续继续推进前，应以 6-C 验收结果作为功能优先级依据。

## 发布判断

当前发布判断：

1. 当前仍不建议打开入口。
2. 当前仍不建议上传体验版。
3. 当前仍不建议部署云函数。
4. 当前仍不建议发布正式版。
5. 当前仍不建议扩大体验范围。

原因：

1. Phase 6-B 是低风险页面提示增强，不等于正式能力成熟。
2. 复制能力仅用于人工反馈采集，不代表已建立产品内反馈闭环。
3. 仍需要真实老板反馈来判断后续优先级。

## 是否建议进入下一阶段

建议进入 Phase 6-D：反馈问题文案微调或体验版再次验证准备。

进入条件：

1. 如果老板认为反馈问题清楚，可准备再次受控体验。
2. 如果老板认为文案不清楚，应先做最小文案修正。
3. 当前仍不建议打开入口。
4. 当前仍不建议上传体验版。
5. 当前仍不建议发布正式版。

## 验证结果

已执行：

```bash
git diff --check
git status
```

检查结论：

1. 本阶段只新增本验收文档。
2. 当前 `ENABLE_V2_DEAL_LOOP_ENTRY = false`。
3. 未修改 `miniprogram/`。
4. 未修改 `cloudfunctions/`。
5. 未修改 `miniprogram/app.json`。
6. 未修改工作台入口。
7. 未修改 tabBar。
8. 未修改其它 V2 页面。
9. 未打开入口。
10. 未上传体验版。
11. 未部署云函数。
12. 未发布正式版。
