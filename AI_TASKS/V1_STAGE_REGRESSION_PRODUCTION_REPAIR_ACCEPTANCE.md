# 透明工地小程序生产修复结果只读验收与审计归档

## 1. 当前阶段

Phase 7-HOTFIX-4C：生产修复结果只读验收与审计归档。

核验日期：2026-07-12。

本阶段只读核验，未执行任何生产写入、回滚或业务云函数调用。

## 2. 修复背景

四个项目曾出现项目汇总工序和进度倒退。本次受控修复只允许更新项目汇总字段 `currentStage` 和 `progress`，并由数据库正常维护 `updatedAt`。

日报、照片、业主绑定、员工关系、状态、状态码和通知均不在修复范围内。

## 3. 代码基线与入口状态

- 分支：`hotfix/stage-regression-and-reviewer-auto-approve`
- 验收前 HEAD：`df9bf8a2dd921bf8a9862f41bd3fd40961a32544`
- 验收后 HEAD：同上
- 工作区：clean
- 本地/远端同步：`0 0`
- `ENABLE_V2_DEAL_LOOP_ENTRY = false`

## 4. 生产只读核验方式

通过微信开发者工具云开发控制台，对 `projects`、`stage_logs`、`operation_logs` 和通知集合进行只读查看。未点击编辑、确定、添加、删除、导入或回滚操作。

生产执行日志仅作为本地核验参考：

`/tmp/stage-regression-production-repair-log.json`

## 5. 四个项目最终结果

| 项目 | 目标值 | 只读回读最终值 | 状态 | 判定 |
|---|---|---|---|---|
| 工地 A | 油工/刮墙 / 80% | 油工/刮墙 / 80% | 施工中 | PASS |
| 工地 B | 水电定位 / 30% | 水电定位 / 30% | 施工中 | PASS_WITH_PENDING_UI_CHECK |
| 工地 C | 开关插座 / 95% | 开关插座 / 95% | 施工中 | PASS_WITH_PENDING_UI_CHECK |
| 工地 D | 木工/吊顶 / 65% | 开工交底 / 10% | 施工中 | FAIL |

四个项目只读回读均确认：租户匹配、`statusCode = in_progress`、`currentStageCode` 不存在。工地 D 的目标工序和进度未在生产集合中生效，不能记为修复完成。

## 6. 修复前值与计划字段

四个项目修复前均为：

- `currentStage`：开工交底
- `progress`：10
- `status`：施工中
- `statusCode`：`in_progress`
- `currentStageCode`：不存在

计划内允许变化：

- `currentStage`
- `progress`
- 数据库正常维护的 `updatedAt`

实际核验结果：A、B、C 的目标字段已生效；D 未发生目标字段变化。未发现计划外字段变化。

`updatedAt` 由数据库系统维护，本次未手工写入；精确时间不进入本脱敏报告。

## 7. 日报、操作日志与通知核验

- `stage_logs`：只读页面仍为 2 页，对应前置导出 44 条；未发现本次修复新增、删除或修改日报的证据。
- `operation_logs`：只读页面仍为 8 页，对应前置导出 200 条；未发现本次修复产生的异常操作记录。
- 通知集合：未发现与本次修复相关的异常通知记录。
- 未调用 `submitStageLog` 或 `reviewStageLog`。
- 未触发订阅消息或企业微信通知。

## 8. 其它项目影响检查

只读回读确认：

- 工地 A：油工/刮墙 / 80%
- 工地 B：水电定位 / 30%
- 工地 C：开关插座 / 95%

未发现其它项目被本轮验收操作改变。工地 D 的问题是目标值未生效，不是其它项目串改。

## 9. 人工界面验收状态

- 工地 A：老板端、工长端、业主端、历史日报均已人工确认通过。
- 工地 B：数据库和保护字段已人工确认；老板端、工长端、业主端完整验收证据待补。
- 工地 C：数据库回读通过；完整真机界面验收证据待补。
- 工地 D：此前收到过人工主字段确认，但与本次生产只读回读冲突，人工界面验收不能视为通过。

## 10. 整体审计结论

整体结论：**D：发现错误，需要立即停止后续上线动作。**

原因：工地 D 生产最终值仍为开工交底 / 10%，未达到木工/吊顶 / 65% 的人工确认目标。

本次不执行自动回滚。A、B、C 不需要回滚；D 需要重新进行生产写入前核验和单项目受控修复后，再重新执行只读验收。

## 11. 根因代码状态

本次数据修复只处理项目汇总数据，未部署以下根因修复：

- 工序防倒退业务代码
- 上传页默认当前工序
- 审核角色本人上传自动审核
- 相关体验版真机回归

本次数据修复不代表根因已经在线上修复。业务代码正式部署前，仍存在再次发生工序倒退或审核卡住的可能。

## 12. 后续必须完成事项

1. 停止上线和 HOTFIX-5 进入动作。
2. 重新核对工地 D 的真实生产记录和人工操作结果。
3. 按单项目 CAS 规则重新执行 D 的受控修复，不执行批量更新。
4. D 修复后立即只读回读项目字段、日报、日志和通知。
5. 补齐 B、C、D 的老板端、工长端、业主端真机验收。
6. 四个项目全部 PASS 后，重新生成验收结论。
7. 根因业务代码另行完成测试、部署准备和体验版回归。

## 13. 隐私与脱敏说明

本报告只使用“工地 A / 工地 B / 工地 C / 工地 D”作为项目标识，不包含完整项目 ID、租户 ID、业主姓名、手机号、openid、详细地址、图片 fileID、生产 JSON 全文或数据库凭证。

## 14. 本阶段安全声明

- 是否连接生产数据库：仅只读查询，通过微信开发者工具云开发控制台。
- 是否执行生产写入：否。
- 是否修改项目：否。
- 是否修改日报：否。
- 是否部署：否。
- 是否上传体验版：否。
- 是否发布正式版：否。
- 是否合并 PR #2：否。
